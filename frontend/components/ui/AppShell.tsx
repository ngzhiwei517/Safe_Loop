import Link from "next/link";
import type { ReactNode } from "react";

export type AppShellNavItem = { href: string; label: string; icon: ReactNode };

export function AppShell({ children, greeting, name, avatar = null, inboxHref, inboxLabel, inboxIcon = null, unreadCount, navItems, activeHref }: { children: ReactNode; greeting: string; name: string; avatar?: ReactNode; inboxHref?: string; inboxLabel: string; inboxIcon?: ReactNode; unreadCount: number; navItems: AppShellNavItem[]; activeHref: string }) {
  return <div className="mx-auto flex min-h-screen max-w-[430px] flex-col bg-bg"><header className="flex items-center gap-3 px-5 pb-2 pt-4"><span className="text-2xl" aria-hidden="true">{avatar}</span><div className="min-w-0 flex-1 text-center"><p className="m-0 text-sm text-inkMuted">{greeting}</p><p className="m-0 text-xl font-bold text-ink">{name}</p></div><Link className="relative grid min-h-11 min-w-11 place-items-center rounded-control border border-border bg-surface" href={inboxHref ?? inboxLabel} aria-label={inboxLabel}>{inboxIcon}{unreadCount > 0 && <span className="absolute -right-1 -top-1 min-w-5 rounded-chip bg-danger px-1 text-center text-xs font-bold text-ink-inverse">{unreadCount}</span>}</Link></header><main className="flex-1 px-5 py-1">{children}</main><nav className="flex border-t border-border bg-bg px-2 py-2">{navItems.map((item) => <Link className={`flex min-h-11 flex-1 flex-col items-center justify-center gap-1 text-xs font-bold ${item.href === activeHref ? "text-primary" : "text-inkMuted"}`} href={item.href} key={item.href}>{item.icon}<span>{item.label}</span></Link>)}</nav></div>;
}
